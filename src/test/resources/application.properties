#de.codecentric.host=https://www.example.org

# This file is of type UTF-8 Unicode (with BOM) text.
# The first sign (#) in the file seems to be ignored when parsed by Spring Boot (or whatever is used inside for reading
# the properties).
# In consequence the value of the property is written to MyProperties.